# Ignore runtime outputs by default: checkpoints, logs, predictions, and data.
*
!.gitignore

# Allow Git to traverse the result directory hierarchy.
!runs/
!runs/**/

# Track only compact, reviewable experiment summaries.
!runs/**/metrics.json
!runs/**/comparison_validation.json
!runs/**/failure_ood_report.json
!runs/**/report.json
